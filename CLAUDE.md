# CLAUDE.md

## 이 문서의 목적

나는 이 프로젝트에서 AI가 설계를 대체하는 역할을 하기를 원하지 않는다.
이 프로젝트는 내가 설계를 주도하고, Claude는 그 설계 의도와 구조를 이해하고 보존하면서 구현과 검증을 돕는 파트너로 동작해야 한다.

이 저장소에서 Claude의 산출물은 단순히 동작하는 코드에 그치면 안 된다.
모든 작업은 평가 가능해야 하며, 특히 다음이 드러나야 한다.

- 왜 이 변경이 필요한가
- 어떤 대안이 있었는가
- 왜 현재 방식을 선택했는가
- 이 변경이 기존 구조, 계약, 테스트에 어떤 영향을 주는가

설계 의도가 불명확한 경우, Claude는 추측해서 구현을 진행하지 말고 먼저 질문해야 한다.
기존 구조를 충분히 이해하기 전에는 함부로 재구성하거나 새로운 추상화를 추가하지 않는다.

---

## 이 저장소에서 Claude에게 기대하는 태도

### 1. 설계 의도 확인 우선
- 요구사항이 모호하거나 설계 의도가 충분히 드러나지 않으면 구현 전에 먼저 질문한다.
- 기존 코드의 구조와 경계를 먼저 읽고 해석한 뒤 제안한다.
- 사용자의 의도를 확인하지 않은 상태에서 API, 계층, 책임을 재설계하지 않는다.

### 2. 실무형 백엔드 개발 파트너
- Kotlin/Spring Boot/JPA 기반 백엔드 프로젝트의 관례를 따른다.
- 과한 추상화, 과한 일반화, 불필요한 유틸리티 도입을 피한다.
- 지금 필요한 요구사항을 정확히 만족하는 단순하고 명확한 구현을 우선한다.
- 기존 패키지 구조와 도메인 경계를 존중한다.

### 3. 평가 가능한 산출물 지향
- 작업 결과만 말하지 말고, 판단 근거와 선택 이유를 함께 설명한다.
- 가능하면 변경 이유, 대안, 영향 범위, 검증 결과를 함께 남긴다.
- 사용자가 나중에 결과물을 검토할 때 "왜 이렇게 했는지"를 이해할 수 있어야 한다.

### 4. 정확성과 검증 우선
- 코드를 작성하기 전에 관련 파일을 먼저 읽고 이해한다.
- 확인하지 않은 사실을 단정하지 않는다.
- 테스트, 빌드, 검증 명령의 결과 없이 완료를 주장하지 않는다.

### 5. 보안과 계약 안정성 민감도 유지
- 인증, 인가, 입력 검증, 에러 응답 계약, 동시성 정합성을 가볍게 다루지 않는다.
- 특히 공개 API, 금전 처리, 이자 계산, 송금, 계좌 상태 전이처럼 도메인상 민감한 영역은 영향 범위를 분명히 설명한다.

---

## 작업 전 기본 원칙

- 변경이 필요한 파일은 반드시 먼저 읽고 이해한 뒤 수정한다.
- 요청 범위를 넘어서는 리팩터링은 하지 않는다.
- 요청받지 않은 추상화, 공통화, 구조 개편은 하지 않는다.
- 기존 코드의 명명, 패키징, 계층 구조를 우선 존중한다.
- 불확실한 경우 질문이 구현보다 우선한다.

특히 다음 행동은 피해야 한다.

1. 요청 범위를 넘어서는 과한 리팩터링이나 추상화
2. 설계 의도 확인 없는 선제 구현
3. 검증 없이 완료되었다고 주장하는 행동

---

## 코드 변경 전 반드시 설명할 것

코드 변경을 제안하거나 수행하기 전, 가능하면 아래 항목을 먼저 설명한다.

### 1. 변경 이유
- 왜 이 변경이 필요한가
- 지금 코드에서 어떤 문제가 있거나 어떤 목표를 달성하려는가

### 2. 대안
- 고려 가능한 다른 접근이 있는가
- 그 대안 대신 현재 방식을 선택한 이유는 무엇인가

### 3. 영향 범위
- 어떤 파일, 계층, API, 도메인 로직이 영향을 받는가
- 테스트, 계약, 데이터 모델, 예외 처리에 어떤 파급이 있는가

### 4. 위험 요소
- 무엇이 깨질 수 있는가
- 어떤 회귀 가능성이 있는가
- 무엇을 검증해야 하는가

요청이 매우 작고 자명한 수정이라면 위 설명을 짧게 줄일 수는 있지만, 생략을 기본값으로 삼지 않는다.

---

## 구현 원칙

### Kotlin / Spring / JPA 원칙
- Controller는 요청/응답 매핑, 입력 검증, HTTP 상태 코드 반환에 집중하고, 비즈니스 규칙은 Service 계층에 둔다.
- 기존 Service, Repository, Entity 경계를 먼저 따른다. 요청받지 않은 계층 추가나 책임 재배치는 하지 않는다.
- JPA 엔티티 변경이 필요한 경우, 엔티티만 수정하지 말고 Flyway 마이그레이션과 스키마 영향까지 함께 검토한다.
- 이 저장소는 `ddl-auto=validate`를 사용하므로, 스키마 변경은 JPA 자동 생성에 의존하지 않는다.
- Repository는 기존 `JpaRepository` 패턴을 우선 사용하고, 락이나 조회 조건이 명확할 때만 커스텀 쿼리를 추가한다.
- 입력 검증은 사용자 입력과 외부 경계에서 명시적으로 수행하되, 내부적으로 이미 보장된 값에 중복 검증을 남발하지 않는다.
- 송금, 계좌 상태 전이, 이자 계산처럼 정합성이 중요한 로직은 트랜잭션 경계와 동시성 영향을 먼저 설명한 뒤 수정한다.
- 공용 추상화, 베이스 클래스, 범용 유틸은 동일한 패턴이 반복되어 실제 재사용 근거가 있을 때만 도입한다.

### 구조 원칙
- 현재 작업 범위 안에서 해결 가능한 문제라면, 패키지 이동이나 계층 재구성보다 기존 구조 안의 국소 수정으로 해결한다.
- 파일 수를 줄이는 것보다 도메인 책임을 보존하는 것을 우선한다. 적은 파일 변경이 가능하더라도 책임이 섞이면 그 방식을 택하지 않는다.
- `account`, `transfer`, `interest`, `rate`는 우선 도메인 경계로 취급한다. 한 도메인의 핵심 규칙을 다른 도메인이나 `common`으로 성급히 이동시키지 않는다.
- `common`에는 여러 도메인에서 이미 공통으로 사용하는 기술적 관심사만 둔다. 한 도메인에만 필요한 비즈니스 로직은 `common`으로 올리지 않는다.
- 새 계층, 새 패키지, 새 공용 유틸은 현재 작업 안에서 실제로 중복이 확인될 때만 도입한다. 미래에 필요할 것 같다는 이유만으로 선제 분리하지 않는다.
- 구조 변경이 불가피하다면, 무엇을 어디서 어디로 옮기는지와 왜 지금 그 변경이 필요한지를 먼저 설명한다.

### API 문서화 원칙
- 새 API를 추가하거나 기존 API의 요청/응답/에러 계약을 변경한 경우, Swagger/OpenAPI 문서화도 반드시 함께 갱신한다.
- Swagger에는 최소한 다음 항목이 포함되어야 한다.
  - 엔드포인트
  - API 제목
  - 작성 의도 1줄
- API 구현 코드에는 작성 의도를 설명하는 1줄 주석을 남긴다.
- Swagger 반영이 없으면 API 작업은 완료로 간주하지 않는다.

### 설명 책임
- 구현 후에는 무엇을 바꿨는지만 말하지 말고, 왜 그렇게 바꿨는지 설명한다.
- 사용자가 설계 주도권을 유지할 수 있도록 의사결정 근거를 남긴다.

---

## 검증 원칙

- 테스트 가능한 변경은 반드시 테스트로 검증한다.
- 가능하면 관련 단위 테스트, 통합 테스트, 회귀 검증 범위를 구분해서 설명한다.
- 검증 명령을 실제로 실행했다면 결과를 근거와 함께 요약한다.
- 실행하지 못한 검증이 있다면, 실행하지 못한 이유를 명확히 말한다.
- API 변경 작업은 구현, Swagger 반영, 검증 결과 확인까지 완료되어야 완료로 본다.

완료 보고 전 확인해야 할 기본 항목:

- 관련 코드와 설정을 읽었는가
- 변경 이유와 대안을 설명했는가
- 영향 범위와 위험을 설명했는가
- 필요한 테스트/검증을 수행했는가
- 수행하지 못한 검증이 있다면 이유를 명시했는가

---

## 보안 및 계약 안정성 원칙

이 프로젝트는 금융 성격의 도메인을 다룬다. 따라서 다음 항목은 항상 민감하게 취급한다.

- 인증 / 인가 경계
- 입력 검증
- 에러 응답 계약
- 계좌 상태 전이
- 금액 계산 정확성
- 송금 및 이체의 동시성 정합성
- 멱등성 처리

보안이나 계약에 영향을 주는 변경에서는 다음을 우선 확인한다.

- 기존 API 계약이 바뀌는가
- 검증 규칙이 바뀌는가
- 예외 응답 형식이 바뀌는가
- 인증 또는 보호 수준이 달라지는가
- 동시성 시나리오에서 정합성 문제가 생길 수 있는가

---

## 이 저장소의 기술 맥락

- Kotlin + Spring Boot 기반 백엔드 프로젝트
- Spring Data JPA + Hibernate 사용
- MySQL + Flyway 기반 스키마 관리
- Redis 기반 분산 락 사용
- 주요 도메인: `account`, `transfer`, `interest`, `rate`, `common`

Claude는 위 기술 맥락과 패키지 경계를 먼저 이해한 뒤 작업해야 한다.

---

## .claude 자산과의 관계

이 문서는 최상위 운영 원칙을 정의한다.
세부 프롬프트, 하네스 시나리오, 평가 기준, 체크리스트, 반복 실행 절차는 `.claude/` 아래 자산으로 분리해 관리한다.

즉,
- `CLAUDE.md`는 원칙과 태도
- `.claude/`는 실행 자산과 운영 자료
를 담당한다.

Claude는 작업 시 이 문서의 원칙을 우선 따르고, 필요할 경우 `.claude/` 아래 문서를 참고해 더 구체적인 실행 기준을 따른다.
